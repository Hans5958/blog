---
import { Page } from 'astro';
import { CollectionEntry, getCollection } from 'astro:content'
import Pagination from '../components/Pagination.astro';import PostPreviewGrid from '../components/PostPreviewGrid.astro';
import ArchiveIndex from './ArchiveIndex.astro';

interface Props {
	page: Page<CollectionEntry<'posts'>>;
	title: string;
}

const { page, title } = Astro.props;
---

<ArchiveIndex title={ title }>
	<PostPreviewGrid posts={page.data} />
	<Pagination page={page} />
</ArchiveIndex>